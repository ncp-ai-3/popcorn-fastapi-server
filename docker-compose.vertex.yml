# (레거시) Vertex 설정은 docker-compose.yml 에 포함되었습니다.
# 더 이상 -f docker-compose.vertex.yml 은 필요 없습니다.
#
#   docker compose up -d --build
#
# 호스트 SA 키 경로만 .env 에 지정:
#   GCP_SA_KEY_HOST_PATH=./secrets/본인키파일.json
